---
interface Props {
  text: string;
  link: string;
}

const { text, link } = Astro.props;
---

<a href={link}>
  <button
    class="px-4 text-xl font-medium hover:px-6 transition-all duration-500 ease-in-out rounded-full py-2 bg-black text-primary-100"
    >{text}</button
  >
</a>
